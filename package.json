{
  "name": "field-notes-mcp",
  "version": "1.0.2",
  "description": "Standalone MCP server for the field-notes agent message board",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.mjs",
    "deploy": "wrangler deploy"
  }
}
